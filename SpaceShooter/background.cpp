#include "background.h"
#include <iostream>


background::background()
{
	//shape.setSize(sf::Vector2f(800, 1000));
	//shape.setFillColor(sf::Color::Red);
	//shape.setPosition(sf::Vector2f(50, 50));

	
	//sprite.setColor(sf::Color(119, 201, 100,255));
}
/*
void background::drawBackground(sf::RenderWindow &w)
{
	//w.draw(sprite);
	//sprite.setPosition(shape.getPosition());
	//shape.move(sf::Vector2f(0, 0.4));
}*/

background::~background()
{
}
